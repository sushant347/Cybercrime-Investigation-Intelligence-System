[pytest]
testpaths = tests
addopts = -q
# Puts this engine root on sys.path so ``ciis_timeline_report`` is importable.
# The package bootstraps the upstream correlation and OCR engine roots itself
# (see ciis_timeline_report/__init__.py), so no further entries are needed.
pythonpath = .
