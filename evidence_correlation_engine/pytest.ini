[pytest]
testpaths = tests
addopts = -q
# Puts this engine root on sys.path so ``ciis_correlation`` is importable.
# The package itself bootstraps the upstream OCR engine root (see
# ciis_correlation/__init__.py), so no second entry is needed here.
pythonpath = .
